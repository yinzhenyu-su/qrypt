// Package control exposes qrypt runtime state through the local debug socket.
//
// It should report snapshots and execute explicit debug operations over VFS and
// drive abstractions without depending on concrete provider packages.
package control
